const { cwd } = require('./config');

// node_modules loader options
module.exports = {
    presets: [
        ['@babel/preset-env', {
            loose: true,
            modules: false,
            configPath: cwd()
        }]
    ],
    plugins: [
        ['@babel/plugin-proposal-class-properties', {
            loose: true
        }]
    ]
};